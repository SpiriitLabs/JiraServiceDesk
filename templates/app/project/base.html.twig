{% trans_default_domain 'app' %}
{% extends 'app/layout.html.twig' %}
{% block title 'project.label'|trans %}

{% set breadcrumb = [{
    label: 'project.label'|trans,
}]|merge(breadcrumb|default([])) %}
