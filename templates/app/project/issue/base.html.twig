{% trans_default_domain 'app' %}
{% extends 'app/project/base.html.twig' %}
{% block title 'issue.label'|trans %}

{% set breadcrumb = [
    {
        href: path('app_project_view', {key: current_project.jiraKey}),
        label: current_project.name,
    },
    {
        href: path('app_project_issue_list', {key: current_project.jiraKey}),
        label: 'issue.label'|trans
    }
]|merge(breadcrumb|default([])) %}
