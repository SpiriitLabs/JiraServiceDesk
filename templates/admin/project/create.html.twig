{% extends 'admin/project/base.html.twig' %}

{% trans_default_domain 'app' %}

{% block pageTitle 'project.title.create'|trans() %}
{% set breadcrumb = [
    {
        label: 'action.create',
    }
] %}

{% block content %}
    <twig:form
        form="{{ form }}"
        cancelLink="{{ path('admin_project_list') }}"
    >
        <twig:block name="content">
            {% include 'admin/project/_form_content.html.twig' %}
        </twig:block>
    </twig:form>
{% endblock %}
